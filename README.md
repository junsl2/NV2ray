# NV2ray

A black Liquid Glass-inspired macOS menu-bar prototype for VLESS profiles, custom encrypted DNS, and rule-based routing.

The current repository contains the SwiftUI interface and configuration layer. A signed Network Extension target and an embedded networking core are still required for a real system-wide tunnel.
